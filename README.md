# bitcoin-vault

A rust lib for working with vault transaction

## Test

## Prerequisite:

- Build wasm:

```
cd wasm && bun run build
```

- Link wasm:

```
cd wasm && bun link
```

- Install dependencies:

```
cd binding && bun i
```

- Test:

```
bun test
```
